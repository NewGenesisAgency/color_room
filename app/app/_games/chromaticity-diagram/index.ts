/**
 * @file app/_games/chromaticity-diagram/index.ts
 * @brief Point d'entrée du module "chromaticity-diagram".
 *
 * Réexporte par défaut le composant {@link ChromaticityDiagram} afin de permettre
 * un import du dossier (`@/app/_games/chromaticity-diagram`) sans préciser le fichier.
 */

export { default } from './ChromaticityDiagram';
